<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.workoutmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WorkoutManager">
        <activity android:name=".workout.view.WorkoutActivity"></activity>
        <activity android:name=".add.view.AddExerciseActivity" />
        <activity
            android:name=".add.view.AddWorkoutActivity"
            android:theme="@style/Theme.WorkoutManager" />
        <activity
            android:name=".home.view.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.WorkoutManager" />
        <activity android:name=".authentication.view.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".authentication.view.RegisterActivity" />
    </application>

</manifest>